<h1> My Contacts </h1>

<% @contacts.each do |contact| %>
<h2><a href="/contacts/<%= contact.id %>"><%= contact.first_name %></a></h2>
  <h2> <%= contact.first_name %></h2> 
  <h3> <%= contact.middlename %> </h3>
  <h2> <%= contact.last_name %></h2> 
  <h3> <%= contact.email %></h3>
  <h3> <%= contact.phone_number %></h3> 
  <br>
  <p> bio <%= contact.bio %></p>
  <br>

  <%end%>
